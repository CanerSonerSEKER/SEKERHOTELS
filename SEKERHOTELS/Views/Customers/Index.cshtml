@model IEnumerable<SEKERHOTELS.Models.CustomersModel>

@{
    ViewData["Title"] = "Müşteriler ";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="text-primary">Müşteriler </h2>
        <a asp-action="CreateAndUpdate" asp-controller="Customers" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Yeni Müşteri Oluştur 
        </a>
    </div>

    <div class="card shadow">
        <div class="card-body">
            @if (Model != null && Model.Any())
            {
                <div class="row row-cols-1 row-cols-md-3 g-4 mt-4">
                    @foreach (var item in Model)
                    {
                        <div class="col">
                            <div class="card h-100 shadow-sm border-0">
                                <div class="card-body">
                                    <h5 class="card-title text-primary">İsim Soyisim : @item.FullName</h5>
                                    <p class="card-text mb-1"><strong>E-mail :</strong> @item.Email</p>
                                    <p class="card-text mb-1"><strong>Telefon Numarası :</strong> @item.Phone</p>
                                </div>
                                <div class="card-footer bg-white border-top-0 d-flex justify-content-between">
                                    <a asp-action="CreateAndUpdate" asp-controller="Customers" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-pencil-square"></i> Düzenle
                                    </a>
                                    <a asp-action="Delete" asp-controller="Customers" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger" onclick="return confirm('Bu oda tipini silmek istediğinizden emin misiniz?');">
                                        <i class="bi bi-trash"></i> Sil
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="alert alert-info text-center">
                    Henüz herhangi bir oda tipi eklenmemiş.
                </div>
            }
        </div>
    </div>
</div>

<!-- Bootstrap Icons (opsiyonel ama ikonları gösterir) -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
